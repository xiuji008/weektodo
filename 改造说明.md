# WeekToDo 改造说明

## 项目背景

本项目基于开源项目 [WeekToDo](https://github.com/manuelernestog/weektodo) 进行二次开发和定制。WeekToDo 是一款免费开源的简约每周计划应用，专注于隐私保护，支持 Windows、Mac、Linux 和 Web 平台。

**原始项目信息：**
- 作者：Manuel Ernesto Garcia
- 开源协议：GNU GPL 3
- 原始仓库：https://github.com/manuelernestog/weektodo
- 项目版本：v2.2.0

## 改造目的

本次改造旨在为 WeekToDo 添加现代化的 AI 能力、增强用户体验，并优化部署流程，使其更适合国内用户使用。

## 主要改造内容

### 1. AI 功能集成 🤖

#### 1.1 AI 服务配置
- 新增 AI 服务配置模块，支持任何 OpenAI 兼容的 API
- 支持的服务商包括：
  - OpenAI
  - DeepSeek
  - Moonshot (月之暗面)
  - Qwen (通义千问)
  - SiliconFlow
  - 自定义 API 端点

#### 1.2 AI 生成周待办
- 在周视图标题栏新增 "AI 生成周待办" 按钮
- 支持基于用户输入快速生成一周的任务计划
- 智能分析任务优先级和时间分配

#### 1.3 AI 生成自定义列表待办
- 在自定义列表的下拉菜单中新增 "AI 生成待办" 菜单项
- 支持基于用户输入为指定自定义列表生成待办事项
- 流式生成体验，实时预览 JSON 结果
- 生成后以卡片形式预览待办，确认后一键应用到列表
- 与周待办共用 AI 配置和提示词
- 支持 emoji、颜色标签、时间、优先级等丰富属性

#### 1.4 AI 对话功能
- 支持对话上下文管理
- 可配置上下文轮数
- 支持连续对话，提升交互体验

**相关文件：**
- `src/helpers/aiService.js` - AI 服务核心逻辑
- `src/repositories/aiConfigRepository.js` - AI 配置数据管理
- `src/store/modules/main.store.js` - AI 相关状态管理
- `src/components/listHeader.vue` - 自定义列表头部（含 AI 生成待办弹框）

---

### 2. S3 数据同步功能 ☁️

#### 2.1 新增 S3 同步模块
- 支持任何兼容 S3 协议的存储服务
- 支持的存储服务包括：
  - AWS S3
  - MinIO（私有部署）
  - Cloudflare R2
  - Backblaze B2
  - 其他 S3 兼容服务

#### 2.2 核心功能
- **手动同步**：支持一键上传数据到云端 / 从云端恢复
- **自动同步**：
  - 启动时自动拉取最新数据（pull on start）
  - 数据变化时自动推送（push on change）
- **历史版本管理**：
  - 支持保存数据快照
  - 支持查看历史版本列表
  - 支持从历史版本恢复数据
  - 支持删除旧版本

#### 2.3 安全特性
- API 凭证仅存储在本地设备
- 不会发送到任何第三方服务
- 支持自定义端点 URL（可对接私有存储）

#### 2.4 配置项说明
- **Endpoint URL** - S3 服务端点地址
- **Region** - 存储区域
- **Bucket Name** - 存储桶名称
- **Object Key** - 数据对象键名
- **Access Key ID** - 访问密钥 ID
- **Secret Access Key** - 安全访问密钥
- **Auto Sync** - 启用自动同步

**相关文件：**
- `src/repositories/s3ConfigRepository.js` - S3 配置数据管理（推测）
- `src/store/modules/main.store.js` - S3 同步状态管理
- `src/assets/languages/en.json` - S3 相关翻译（100+ 字符串）

---

### 3. 心情跟踪功能 😊

#### 2.1 新增心情跟踪器组件
- 在日历视图下方新增心情跟踪器
- 支持为一周的每一天记录心情
- 每天可记录多个心情表情

#### 2.2 功能特性
- 内置多种心情表情选择
- 支持右键删除心情
- 高亮显示今天和周末
- 悬停显示心情描述

**相关文件：**
- `src/components/moodTracker.vue` - 心情跟踪器组件

---

### 4. 周总结功能 📊

#### 3.1 新增周总结组件
- 在自定义列表区域上方新增周总结视图
- 自动统计本周任务完成情况
- 提供可视化的完成度展示

#### 3.2 功能特性
- 显示本周日期范围
- 统计任务完成率
- 支持展开/收起详细信息

**相关文件：**
- `src/components/weeklySummary.vue` - 周总结组件

---

### 5. Docker 部署优化 🐳

#### 4.1 Dockerfile 改进
- 使用 Node.js 16 Alpine 作为基础镜像
- 配置淘宝镜像源加速 Electron 下载
- 优化构建流程

```dockerfile
ENV ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
```

#### 4.2 Docker Compose 配置
- 配置阿里云容器镜像服务
- 支持一键部署
- 健康检查配置

**相关文件：**
- `Dockerfile`
- `docker-compose.yml`

---

### 6. GitHub Pages 自动部署 🚀

#### 5.1 GitHub Actions 工作流程
- 配置自动构建和部署流程
- 推送到 main 分支自动触发部署
- 使用 GitHub Pages 托管 Web 版本

#### 5.2 部署配置
- Node.js 20 运行环境
- Yarn 包管理器
- 构建产物自动发布到 GitHub Pages

**相关文件：**
- `.github/workflows/deploy-gh-pages.yml`

---

### 7. 用户体验优化 ✨

#### 6.1 浏览器自动填充修复
- 添加隐藏的假登录表单
- 防止浏览器自动填充干扰任务输入

#### 6.2 界面优化
- 优化深色主题显示效果
- 改进响应式布局
- 增强组件动画效果

#### 6.3 多语言支持增强
- 完善中文简体/繁体翻译
- 新增 AI 功能相关翻译
- 新增心情跟踪相关翻译

**相关文件：**
- `src/assets/languages/zh-CN.json` - 中文简体
- `src/assets/languages/zh-TW.json` - 中文繁体
- `src/assets/languages/en.json` - 英语

---

## 技术栈

### 核心框架
- **Vue 3.2.26** - 渐进式 JavaScript 框架
- **Vuex 4.0.2** - 状态管理
- **Vue Router** - 路由管理（如需）
- **Bootstrap 5.3.0** - UI 组件库
- **Electron 25.0.1** - 桌面应用封装

### 新增依赖
- **axios** - HTTP 客户端（用于 AI API 调用）
- **lunar-javascript** - 农历计算
- **markdown-it** - Markdown 渲染

### 开发工具
- **Vue CLI 4.5.15** - 项目脚手架
- **Electron Builder** - Electron 打包工具
- **Sass** - CSS 预处理器

---

### 8. 待办优先级系统 📋

#### 8.1 新增 L1-L5 五级优先级
- 替代旧的 0/1/2 三级优先级，引入完整的 L1~L5 五级定义
- 支持在待办详情弹框中快速切换优先级
- 默认优先级为 L3（本周排期）

#### 8.2 优先级等级定义
| 等级 | 标签 | 处理时效 | 视觉颜色 |
|------|------|----------|---------|
| **L1** | 今日必保 | 今天必须完成 | 🔴 红色 |
| **L2** | 今日主攻 | 今天优先推进 | 🟠 橙色 |
| **L3** | 本周排期 | 本周内完成即可 | 🔵 蓝色 |
| **L4** | 有空再做 | 本月内考虑 | 🟢 绿色 |
| **L5** | 未来待定 | 不承诺本月完成 | ⚪ 灰色 |

#### 8.3 视觉效果
- 不同优先级采用不同字号：L1(1.05rem) → L2(0.95rem) → L3(0.865rem) → L4(0.78rem) → L5(0.72rem)
- 待办项前面显示带颜色的优先级徽章
- 悬浮面板和详情弹框均显示优先级标识

#### 8.4 AI 生成集成
- AI 生成待办时根据任务性质智能分配 L1~L5 优先级
- AI Prompt 中补充完整的优先级定义说明
- 兼容旧数据中的 `priority` 字段（自动映射）

**相关文件：**
- `src/components/toDoItem.vue` - 优先级徽章和字号
- `src/components/activeToDo.vue` - 悬浮面板优先级显示
- `src/views/toDoModal/toDoModal.vue` - 优先级选择器
- `src/repositories/aiConfigRepository.js` - AI Prompt 优先级定义

---

### 9. 功能清理 🧹

#### 9.1 移除 Emoji 功能
- 移除了悬浮面板中的 Emoji 选择按钮和选择器面板
- 移除了待办项中的 Emoji 显示
- 移除了 AI 生成预览中的 Emoji 字段
- 清除相关的 CSS 样式和事件处理代码（约 200 行）
- 保留数据模型中的 `emoji` 字段以确保已有数据不受影响

---

## 功能对比（更新后）

| 功能模块 | 原始版本 | 改造后版本 |
|---------|---------|-----------|
| AI 功能 | ❌ 不支持 | ✅ 支持多种 AI 服务 |
| S3 数据同步 | ❌ 不支持 | ✅ 支持多平台 S3 兼容存储 |
| 心情跟踪 | ❌ 不支持 | ✅ 支持每日心情记录 |
| 周总结 | ❌ 不支持 | ✅ 自动统计任务完成情况 |
| 待办优先级 | ⚠️ 三级（0/1/2） | ✅ 五级 L1~L5 带颜色和字号 |
| Docker 部署 | ⚠️ 基础支持 | ✅ 优化国内镜像源 |
| 自动部署 | ❌ 不支持 | ✅ GitHub Pages 自动部署 |
| Emoji 功能 | ✅ 支持 | ❌ 已移除 |
| 浏览器兼容 | ⚠️ 有自动填充问题 | ✅ 已修复 |
| 中文支持 | ✅ 支持 | ✅ 进一步完善 |

---

## 如何使用

### 1. 本地开发

```bash
# 克隆项目
git clone https://github.com/你的用户名/weektodo.git
cd weektodo

# 安装依赖
yarn install

# 启动开发服务器
yarn serve        # Web 版本
yarn electron:serve  # 桌面版本
```

### 2. Docker 部署

```bash
# 使用 Docker Compose 启动
docker-compose up -d

# 访问应用
open http://localhost:38080
```

### 3. 配置 AI 功能

1. 打开应用设置
2. 找到 "AI 服务" 配置项
3. 填入 API Endpoint、API Key 和模型名称
4. 保存配置后即可使用 AI 功能

### 4. GitHub Pages 部署

推送代码到 main 分支后，GitHub Actions 会自动构建并部署到 GitHub Pages。

---

## 新增配置项说明

### AI 服务配置

```json
{
  "aiEndpoint": "https://api.openai.com/v1",
  "aiApiKey": "your-api-key",
  "aiModel": "gpt-3.5-turbo",
  "aiCarryContext": true,
  "aiContextRounds": 10
}
```

### S3 同步配置

```json
{
  "s3Endpoint": "https://s3.amazonaws.com",
  "s3Region": "us-east-1",
  "s3Bucket": "my-weektodo-bucket",
  "s3ObjectKey": "weektodo-data.json",
  "s3AccessKeyId": "your-access-key-id",
  "s3SecretAccessKey": "your-secret-access-key",
  "s3AutoSync": true
}
```

**配置说明：**
- `s3Endpoint` - S3 服务端点（MinIO 示例：`http://localhost:9000`）
- `s3Region` - 存储区域（MinIO 可留空或填 `us-east-1`）
- `s3Bucket` - 存储桶名称（需提前创建）
- `s3ObjectKey` - 数据文件名
- `s3AutoSync` - 是否启用自动同步

### 心情跟踪数据格式

```json
{
  "moodData": {
    "2026-07-02": ["😊", "🎉"],
    "2026-07-03": ["😌"]
  }
}
```

---

## 目录结构变化

```
src/
├── components/
│   ├── moodTracker.vue          # 新增：心情跟踪器
│   ├── weeklySummary.vue        # 新增：周总结
│   └── ...                      # 其他原有组件
├── helpers/
│   ├── aiService.js             # 新增：AI 服务
│   └── ...
├── repositories/
│   ├── aiConfigRepository.js    # 新增：AI 配置仓库
│   ├── s3ConfigRepository.js    # 新增：S3 配置仓库（推测）
│   └── ...
└── ...
```

**新增配置文件：**
- S3 同步配置存储在浏览器本地存储中
- 配置密钥仅保存在本地，不会上传到云端

---

## 待优化事项

1. **AI 功能**
   - [ ] 支持更多 AI 服务商
   - [ ] 增加 AI 生成内容的历史记录
   - [ ] 支持自定义 AI Prompt

2. **S3 数据同步**
   - [ ] 支持更多云存储协议（WebDAV、Dropbox、Google Drive 等）
   - [ ] 支持冲突解决策略配置
   - [ ] 支持增量同步
   - [ ] 增加同步状态指示器
   - [ ] 支持端到端加密

3. **心情跟踪**
   - [ ] 支持心情数据统计图表
   - [ ] 支持心情趋势分析
   - [ ] 增加更多心情表情

4. **周总结**
   - [ ] 支持导出 PDF 报告
   - [ ] 支持邮件发送周报
   - [ ] 增加更多统计维度

5. **部署优化**
   - [ ] 支持 Docker 多架构构建
   - [ ] 优化 GitHub Actions 构建速度
   - [ ] 支持更多部署平台

---

## 贡献指南

欢迎提交 Issue 和 Pull Request 来改进本项目。

### 开发规范
- 遵循 Vue 3 风格指南
- 使用 ESLint 进行代码检查
- 提交前请运行 `yarn lint`

### 翻译贡献
如需添加新的语言支持，请参考 `src/assets/languages/en.json` 的格式创建新的语言文件。

---

## 许可证

本项目遵循 GNU GPL 3 开源协议，与原始项目保持一致。

- 原始项目版权：Manuel Ernesto Garcia
- 改造部分版权：根据你的实际情况填写

---

## 致谢

感谢原始项目作者 Manuel Ernesto Garcia 的开源贡献。

Original project: [WeekToDo](https://github.com/manuelernestog/weektodo)

---

## 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 GitHub Issue
- 发送邮件至：[你的邮箱]

---

**最后更新时间：** 2026-07-08
